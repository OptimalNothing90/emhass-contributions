# AC-2b runtime_params.json Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add `src/emhass/static/data/runtime_params.json` — a machine-readable schema for EMHASS's runtime-only optimization input knobs — without touching `param_definitions.json` or the GUI.

**Architecture:** A new static JSON data file holding ~12 runtime-only optimization knobs in the AC-2a entry convention (`friendly_name`/`Description`/`input`/`default_value`/`unit`), NO `applies_to` (per-action applicability is encoded later by AM-1b's openapi operations). `def_load_config` is listed as an opaque `object` with an `externalDocs` pointer to `thermal_battery.md` (deep thermal sub-schema, not inlined). A validation test guards structure. `param_definitions.json`, the GUI (`configuration_script.js`), and `config_defaults.json` are untouched.

**Tech Stack:** JSON data file; Python stdlib `json` + `unittest`/`pytest`; `ruff`.

---

## Source of truth

- **Spec:** `docs/superpowers/specs/2026-05-29-ac-2b-design.md` (read first).
- **Research/classification:** `audits/2026-05-29-runtime-schema-family.md` (the full runtime-param bucket map — Bucket A = this file).
- **Runs in the fork** `OptimalNothing90/emhass` at `C:/Users/MauricioSchäpers/claude-code/emhass`, NOT in emhass-contributions. The spec/audit live in the sibling emhass-contributions repo — `cat` them from there.
- Convention: `develop_ai_coders.md` (this is structurally a discussion-first change; **user chose PR-first** — the PR body must justify the new file + offer the `param_definitions` `"Runtime"`-category alternative + note the ladder was considered).
- Unblocks AM-1b. NOT gated on AC-4 (standalone file).

---

## File Structure

| File | Responsibility | Action |
|---|---|---|
| `src/emhass/static/data/runtime_params.json` | Runtime-only optimization-knob schema (~12 entries, grouped) | Create |
| `tests/test_runtime_params.py` | Structural validation (parse / required keys / input+unit enums / no applies_to / key-presence) | Create |
| `docs/passing_data.md` | One-line cross-reference to the machine schema | Modify (append a sentence) |

---

## Task 1: Completeness gate — full `treat_runtimeparams` source scan (do this FIRST)

**Files:** none (analysis; output a classification note appended to this plan).

This locks the exact entry set against source truth BEFORE writing the file. The earlier
`runtimeparams.get/[]/in` regex was partial — it missed the `forecast_key` list and the
`ml_param_defs` table.

- [ ] **Step 1: Read the spec + audit**

```bash
cat ../emhass-contributions/docs/superpowers/specs/2026-05-29-ac-2b-design.md
cat ../emhass-contributions/audits/2026-05-29-runtime-schema-family.md
```

- [ ] **Step 2: Enumerate EVERY runtime key from source**

Open `src/emhass/utils.py`, find `treat_runtimeparams`. Enumerate every accepted key across:
- all `if "<key>" in runtimeparams` / `runtimeparams.get("<key>")` / `runtimeparams["<key>"]` sites,
- the **`forecast_key`** list (forecast inputs),
- the **`ml_param_defs`** table (ML fit/predict/tune params),
- the `default_passed_dict` (publish/custom_*_id defaults),
- any `set_type ==` gated branches (these tell you action applicability).

- [ ] **Step 3: Classify each key into the audit's buckets**

For each enumerated key assign a bucket (audit §2):
A runtime-only optim knob (→ this file) · B output routing (→ AC-2c) · C override of an existing
`param_definitions` entry (→ AM-1b refs, no new entry) · D data/ML payload (→ AM-1b) ·
E §4.1 config key (→ AM-7) · F secret (excluded) · G legacy alias (excluded).
**Any key that fits no bucket → STOP and surface to the main session** (don't silently drop).

- [ ] **Step 4: Resolve the borderlines (from spec §5)**
- `time_zone`: is it read from a config param (→ bucket C, exclude) or genuinely runtime-only (→ A)? Decide from source.
- `adjusted_pv_model_max_age`, `open_meteo_cache_max_age`: confirm runtime-only (not a config_defaults / param_definitions entry).
- `def_load_config`: confirm include-as-linked-object (recommended) vs omit.

- [ ] **Step 5: Write the locked set**

Append a `## Completeness-scan result (AC-2b)` section to THIS plan file listing: the final
Bucket-A set (the entries that go in `runtime_params.json`), and a one-line bucket assignment
for every other runtime key (proof of coverage). This locked set drives Tasks 2–3.

- [ ] **Step 6: Commit the analysis**

```bash
git add ../emhass-contributions/docs/superpowers/plans/2026-05-29-ac-2b.md  # if writable; else keep in PR notes
git commit -m "docs: AC-2b completeness-scan result" || true
```
(If the sibling repo isn't part of this fork's git, keep the scan result in the PR body instead.)

---

## Task 2: Validation test (failing first)

**Files:** Create `tests/test_runtime_params.py`

- [ ] **Step 1: Write the test**

```python
import json
import unittest
from pathlib import Path

REPO = Path(__file__).resolve().parents[1]
RP = REPO / "src" / "emhass" / "static" / "data" / "runtime_params.json"

VALID_INPUT = {
    "int", "float", "boolean", "string", "object", "select", "time",
    "array.int", "array.float", "array.boolean", "array.string", "array.time", "array.array.float",
}
VALID_UNIT = {
    "W", "Wh", "kWh", "¤/kWh", "€", "%", "fraction", "°C", "°",
    "min", "h", "days", "timesteps", "count", "s", "none",
}
# Finalize this set from the Task 1 completeness scan before marking the task done.
EXPECTED_KEYS = {
    "prediction_horizon", "soc_init", "soc_final",
    "operating_timesteps_of_each_deferrable_load", "alpha", "beta",
    "weather_forecast_cache", "weather_forecast_cache_only",
    "adjusted_pv_model_max_age", "open_meteo_cache_max_age",
    "def_current_state", "def_load_config",
}


def _entries():
    data = json.loads(RP.read_text(encoding="utf-8"))
    for section, params in data.items():
        assert isinstance(params, dict), f"section {section} is not an object"
        for key, val in params.items():
            yield key, val


class TestRuntimeParams(unittest.TestCase):
    def test_file_parses(self):
        self.assertTrue(RP.exists(), f"missing {RP}")
        json.loads(RP.read_text(encoding="utf-8"))

    def test_required_fields(self):
        for key, val in _entries():
            for field in ("friendly_name", "Description", "input", "default_value", "unit"):
                self.assertIn(field, val, f"{key} missing {field}")

    def test_input_and_unit_enums(self):
        for key, val in _entries():
            self.assertIn(val["input"], VALID_INPUT, f"{key} bad input {val['input']!r}")
            self.assertIn(val["unit"], VALID_UNIT, f"{key} bad unit {val['unit']!r}")

    def test_no_applies_to(self):
        # applicability lives in the openapi operations (AM-1b), not the data file
        for key, val in _entries():
            self.assertNotIn("applies_to", val, f"{key} must not carry applies_to")

    def test_expected_keys_present(self):
        keys = {k for _s, params in json.loads(RP.read_text(encoding="utf-8")).items() for k in params}
        missing = EXPECTED_KEYS - keys
        self.assertEqual(missing, set(), f"missing expected keys: {missing}")
```

- [ ] **Step 2: Run, verify it fails**

Run: `python -m pytest tests/test_runtime_params.py -v`
Expected: FAIL — file missing.

- [ ] **Step 3: Commit the test**

```bash
git add tests/test_runtime_params.py
git commit -m "test: runtime_params.json schema validation (AC-2b)"
```

---

## Task 3: Create `runtime_params.json`

**Files:** Create `src/emhass/static/data/runtime_params.json`

- [ ] **Step 1: Write the file** (grouped like `param_definitions.json`; reconcile the exact set against Task 1; Descriptions are sourced from the docs and may be sharpened verbatim)

```json
{
  "MPC": {
    "prediction_horizon": {
      "friendly_name": "Prediction horizon",
      "Description": "Number of timesteps the naive-MPC optimization looks ahead. See passing_data.md / study_cases/mpc.md.",
      "input": "int",
      "default_value": 10,
      "unit": "timesteps"
    },
    "soc_init": {
      "friendly_name": "Initial battery state of charge",
      "Description": "Current battery SOC as a fraction in [0,1] (NOT percent). Clamped to [battery_minimum_state_of_charge, battery_maximum_state_of_charge]; out-of-bounds warns and falls back to battery_target_state_of_charge. See cookbook/battery_aware_runtime_params.md.",
      "input": "float",
      "default_value": null,
      "unit": "fraction"
    },
    "soc_final": {
      "friendly_name": "Final battery state of charge",
      "Description": "Target battery SOC (fraction 0..1) at the end of the horizon. Falls back to battery_target_state_of_charge if omitted; independent of soc_init.",
      "input": "float",
      "default_value": null,
      "unit": "fraction"
    },
    "operating_timesteps_of_each_deferrable_load": {
      "friendly_name": "Operating timesteps of each deferrable load",
      "Description": "Per-deferrable-load total operating timesteps (preferred over operating_hours for sub-hour steps). List, one int per load.",
      "input": "array.int",
      "default_value": null,
      "unit": "timesteps"
    }
  },
  "Cost function": {
    "alpha": {
      "friendly_name": "Alpha (forecast weight)",
      "Description": "Weight on the forecast first element in the now/current mixing model: P_mix = alpha*forecast(t) + beta*measured(t-1). See forecasts.md.",
      "input": "float",
      "default_value": 0.5,
      "unit": "fraction"
    },
    "beta": {
      "friendly_name": "Beta (measured weight)",
      "Description": "Weight on measured persistence in the now/current mixing model (complement to alpha). See forecasts.md.",
      "input": "float",
      "default_value": 0.5,
      "unit": "fraction"
    }
  },
  "Forecast cache": {
    "weather_forecast_cache": {
      "friendly_name": "Weather forecast cache (write)",
      "Description": "If true, save the computed weather/PV forecast to a cache file for later MPC reuse (avoids re-hitting the forecast API). See forecasts.md.",
      "input": "boolean",
      "default_value": false,
      "unit": "none"
    },
    "weather_forecast_cache_only": {
      "friendly_name": "Weather forecast cache only",
      "Description": "If true, only use cached forecast data and error if the cache is missing (prevents accidental API calls when offline).",
      "input": "boolean",
      "default_value": false,
      "unit": "none"
    },
    "adjusted_pv_model_max_age": {
      "friendly_name": "Adjusted-PV model max age",
      "Description": "Max age (hours) of the cached PV-adjustment ML model before re-fitting. 0 = re-fit every call.",
      "input": "float",
      "default_value": 24,
      "unit": "h"
    },
    "open_meteo_cache_max_age": {
      "friendly_name": "Open-Meteo cache max age",
      "Description": "Max age (minutes) of the cached Open-Meteo weather response before re-fetching.",
      "input": "int",
      "default_value": 30,
      "unit": "min"
    }
  },
  "Deferrable runtime": {
    "def_current_state": {
      "friendly_name": "Deferrable current state",
      "Description": "Per-deferrable-load boolean list: is each load currently running at t0? Avoids penalizing a start when a load is already on.",
      "input": "array.boolean",
      "default_value": null,
      "unit": "none"
    },
    "def_load_config": {
      "friendly_name": "Deferrable load special config",
      "Description": "Per-deferrable-load special configuration (thermal_config / thermal_battery models). Deep nested schema, not inlined here.",
      "input": "object",
      "default_value": null,
      "unit": "none",
      "externalDocs": {
        "description": "Thermal load / thermal-battery configuration schema",
        "url": "docs/thermal_battery.md"
      }
    }
  }
}
```

(If Task 1 finds `time_zone` is runtime-only, add it to a `Misc` section as `string`/`null`/`none`.
If a borderline resolves to "exclude", drop it from `EXPECTED_KEYS` in the test too.)

- [ ] **Step 2: Run the test, verify pass**

Run: `python -m pytest tests/test_runtime_params.py -v`
Expected: PASS (all 5 tests).

- [ ] **Step 3: Commit**

```bash
git add src/emhass/static/data/runtime_params.json
git commit -m "feat: add runtime_params.json runtime-input schema (AC-2b)"
```

---

## Task 4: passing_data.md cross-reference

**Files:** Modify `docs/passing_data.md`

- [ ] **Step 1: Add a one-line pointer**

Near the top of the "Passing other data at runtime" section (or the intro), add a sentence:

```markdown
> A machine-readable schema of the runtime optimization parameters is committed at
> `src/emhass/static/data/runtime_params.json` (consumed by the generated OpenAPI spec).
```

- [ ] **Step 2: Commit**

```bash
git add docs/passing_data.md
git commit -m "docs: cross-reference runtime_params.json from passing_data.md (AC-2b)"
```

---

## Task 5: Full verification + draft PR

**Files:** none

- [ ] **Step 1: Full suite**

Run: `python -m pytest -q`
Expected: PASS (no regressions). Note any pre-existing unrelated failures vs a clean master baseline; do not fix here.

- [ ] **Step 2: ruff (no `uvx` — `python -m ruff`)**

```bash
python -m ruff format --check tests/test_runtime_params.py
python -m ruff check tests/test_runtime_params.py
```
Expected: clean. (The JSON file is not ruff's concern; consider `python -m json.tool` to confirm valid JSON.)

- [ ] **Step 3: Confirm untouched files**

```bash
git diff --name-only upstream/master..HEAD
```
Expected: ONLY `src/emhass/static/data/runtime_params.json`, `tests/test_runtime_params.py`, `docs/passing_data.md` (+ the plan/scan note if committed). `param_definitions.json`, `configuration_script.js`, `config_defaults.json` MUST NOT appear.

- [ ] **Step 4: AGENTS.md compliance + Gate 0 simplify** (see Handoff-Prompt).

- [ ] **Step 5: Push + draft PR** — body MUST: justify the new file (GUI renders every param_definitions section as a form field), offer the `param_definitions` `"Runtime"`-category alternative, note the develop_ai_coders.md discussion-first ladder was considered (PR-first chosen), and paste the Task 1 completeness-scan result (proof every runtime key is classified). See Handoff-Prompt for the `gh pr create`.

- [ ] **Step 6: Report** via `HANDOFF-RESULT AC-2b`.

---

## Self-Review (author checklist — completed at write time)

- **Spec coverage:** D#1 scope (Task 1 gate + Task 3), D#2 new file/GUI-untouched (Task 3 + Task 5 Step 3 guard), D#3 no applies_to (Task 2 `test_no_applies_to`), D#4 def_load_config linked (Task 3 entry), D#5–9 routing to AC-2c/AM-1b/AM-7/excluded (Task 1 classification), D#10 schema-only/un-gated (no AC-4 dep), D#11 semver (noted; no version bump needed for an additive new file — note in PR), D#12 PR-first + justification (Task 5 Step 5). Covered.
- **Placeholder scan:** no TBD; the one analysis task (Task 1) is concrete (enumerate → classify → lock); test + JSON are complete.
- **Type consistency:** `EXPECTED_KEYS` in the test matches the Task 3 JSON keys; `VALID_INPUT`/`VALID_UNIT` match the spec's vocab/enum; section-grouped iteration matches the grouped JSON shape.

---

## Execution Handoff

**Not the standard subagent/inline choice.** Per the cross-repo-flow contract + `feedback_never_self_dispatch_fork_work`, the user runs this in a dedicated fork-repo session; the main session appends `## Handoff-Prompt` below and stops. The fork session executes Tasks 1–5 via `superpowers:executing-plans` (or subagent-driven), then reports `HANDOFF-RESULT AC-2b`.

---

## Handoff-Prompt

**Copy-paste into a NEW Claude Code session opened in `C:/Users/MauricioSchäpers/claude-code/emhass/` (the fork):**

````
You are a fork-session for emhass upstream PR work. The main planning session lives in
`C:/Users/MauricioSchäpers/claude-code/emhass-contributions/`. You operate ONLY here in
the `emhass` fork repo.

## AGENTS.md is your rulebook
Before Task 1, read `AGENTS.md` (`cat AGENTS.md`). Follow §3 don't-touch invariants, §4 scope
corridors, §5 limits/gotchas, §6 Karpathy guardrails (Think first / Simplicity / Surgical /
Goal-driven), §7 conventions (commit prefixes, Diátaxis). Note: develop_ai_coders.md routes a
new schema file/convention to discussion-first; this PR goes PR-first by user decision — the PR
body must justify it + offer the alternative (below).

## Item context
- Board ID: AC-2b
- Issue: None (board draft)
- Goal-fit: LLM-ready
- Spec: `docs/superpowers/specs/2026-05-29-ac-2b-design.md`
- Plan: `docs/superpowers/plans/2026-05-29-ac-2b.md`
- Research/classification audit: `audits/2026-05-29-runtime-schema-family.md`

Read them from the sibling repo:
  cat ../emhass-contributions/docs/superpowers/specs/2026-05-29-ac-2b-design.md
  cat ../emhass-contributions/docs/superpowers/plans/2026-05-29-ac-2b.md
  cat ../emhass-contributions/audits/2026-05-29-runtime-schema-family.md

## Pre-flight (mandatory, in order)
1. `gh auth status` — must show `OptimalNothing90` (`gh auth switch --user OptimalNothing90` if not).
2. `git fetch upstream && git checkout master && git reset --hard upstream/master`
3. `git checkout -b feat/ac-2b-runtime-params`
4. `git status` empty before edits.

## Implementation
Use `superpowers:executing-plans` (or `superpowers:subagent-driven-development`).
Plan: `../emhass-contributions/docs/superpowers/plans/2026-05-29-ac-2b.md`. Tasks 1–5.

**Task 1 is a MANDATORY completeness gate before writing the file** — full `treat_runtimeparams`
source scan (ALL `set_type` branches + the `forecast_key` list + the `ml_param_defs` table; the
regex used in research was partial). Classify EVERY runtime key into the audit's buckets; any key
that fits no bucket → STOP and surface to the main session. Lock the Bucket-A set, then write.

Key invariants (do not drift):
- NEW file `src/emhass/static/data/runtime_params.json` only. `param_definitions.json`,
  `configuration_script.js`/GUI, and `config_defaults.json` MUST stay untouched.
- AC-2a entry convention; **NO `applies_to`** field (per-action applicability is AM-1b's openapi job).
- `def_load_config` = `input:"object"` + `externalDocs` → thermal_battery.md (do NOT inline the thermal schema).
- `unit` ∈ the locked enum; Descriptions sourced from the docs (passing_data.md / forecasts.md / cookbook / mpc.md).
- Resolve borderlines in the scan: `time_zone` (config-override→exclude vs runtime-only→include),
  `adjusted_pv_model_max_age`/`open_meteo_cache_max_age` (confirm runtime-only), `def_load_config` (include-linked vs omit).

## Gate 0 — Pre-PR quality pass (MANDATORY before push)
After tasks complete + tests pass, BEFORE push, run the `simplify` skill on the diff
(`git diff --name-only upstream/master..HEAD`). In-scope+surgical → apply (separate commit
`refactor: simplify pass on <files>`); out-of-scope/speculative → reject + log under
`## Simplify pass — rejected suggestions` in the plan file. Zero applicable → log
`Simplify pass: 0 in-scope changes (AC-2b)`.

## PR creation — DRAFT FIRST
  git push -u origin feat/ac-2b-runtime-params
  gh pr create \
    --draft \
    --repo davidusb-geek/emhass \
    --base master \
    --head OptimalNothing90:feat/ac-2b-runtime-params \
    --title "feat: add runtime_params.json (machine-readable runtime param schema)" \
    --body-file - <<'EOF'
## What

Adds `src/emhass/static/data/runtime_params.json` — a machine-readable schema for EMHASS's
runtime-only optimization parameters (the per-`/action`-call knobs `treat_runtimeparams` accepts),
in the same entry convention as `param_definitions.json`. ~12 entries: `prediction_horizon`,
`soc_init`/`soc_final`, `operating_timesteps_of_each_deferrable_load`, `alpha`, `beta`,
`weather_forecast_cache`(`_only`), `adjusted_pv_model_max_age`, `open_meteo_cache_max_age`,
`def_current_state`, `def_load_config`. `def_load_config` is an opaque `object` with an
`externalDocs` link to `thermal_battery.md` (deep thermal sub-schema). + a validation test +
a one-line cross-reference in `passing_data.md`.

## Why

Runtime params are documented today only as prose (`passing_data.md` / `forecasts.md` / …) —
parse-hostile for tool/LLM consumers (Node-RED, EVCC, IDE tooling) and for a generated OpenAPI
spec. This commits a parse-friendly contract.

## Design notes (please weigh in)

- **Separate file, not a `param_definitions.json` "Runtime" category** — because the Web config
  GUI renders every `param_definitions` section as an editable form field
  (`configuration_script.js`), so runtime (per-call) params can't live there. **Happy to switch
  to a `"Runtime"` category + a one-line GUI skip if you prefer fewer files** — flagging this as
  the main convention decision.
- **No `applies_to` field** — per-action applicability belongs in the OpenAPI operations; a
  follow-up composes per-`/action` request bodies from this file (+ a sibling output schema).
- This is structurally a convention change (develop_ai_coders.md → discussion-first); opened as a
  concrete PR for a faster decision — say the word if you'd rather a Discussion.
- `param_definitions.json`, the GUI, and `config_defaults.json` are untouched.

## Completeness

Scanned the full `treat_runtimeparams` (all `set_type` branches + `forecast_key` list +
`ml_param_defs` table) and classified every runtime key. Deliberately NOT in this file:
output/publish routing (`custom_*_id`, `publish_prefix`, `entity_save`), per-call data payloads
(forecast lists) + ML args, runtime overrides of existing config params, secrets, and legacy
aliases — each belongs to the config schema / a separate output schema / the `/action` payload
schema. [Full classification table: paste the Task-1 scan result here.]

## Out of scope

Output/publish routing schema; the `/action` OpenAPI wiring; config-param runtime-overrides;
secrets; legacy aliases — separate follow-ups.
EOF

## Mark-ready (after CI + alerts triaged)
Do NOT `gh pr ready` until: all CI green (`gh pr checks <pr> --watch`); CodeQL 0 alerts OR each
fixed/triaged-with-reply (no unilateral dismissal — main co-signs); Sourcery comments applied OR
replied "won't-fix because X"; self-review walk of the full diff; "Out of scope" items absent.
Then `gh pr ready <pr>`. Emit HANDOFF-RESULT only AFTER ready succeeds. CI fails / unresolved →
HANDOFF-RESULT `status: blocked` with the draft URL in `notes`.

## Return contract
```
HANDOFF-RESULT AC-2b
status: pr-open | blocked | failed
pr-url: <url-or-none>
branch: feat/ac-2b-runtime-params
tests: pass | fail | skipped
notes: <one-line summary OR pivot reason if blocked>
```

## Pivot trigger (if plan wrong)
Plan ≠ upstream reality: do NOT improvise / push partial. Append `## Pivot Reason` (file:line
citations) to `../emhass-contributions/docs/superpowers/plans/2026-05-29-ac-2b.md`, set status
`blocked`. Likely pivot points: a runtime key the scan can't classify; `def_load_config` shape;
the GUI-rendering assumption (verify `configuration_script.js` still iterates all sections).

## Out of scope (this session)
- Spec/audit edits — main session only. Board mutations — main session. Account switch-back — main session.
- AC-2c (runtime_output.json), AM-1b (/action openapi), AM-7 (config-key alignment) — separate cards.

## Session resumability — DO NOT close after HANDOFF-RESULT
Keep this session OPEN. Pivots / review feedback resume THIS session via `claude --resume` from
`C:/Users/MauricioSchäpers/claude-code/emhass/` (pick most-recent AC-2b session) — never fresh.
````
