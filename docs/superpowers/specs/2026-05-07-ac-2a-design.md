> _Self-test artefact (cross-repo-flow). Do not treat as live work._

# AC-2a — Add structured 'unit' field to param_definitions.json — Design

**Date:** 2026-05-07
**Card:** `AC-2a` (board/items.json)
**Issue:** https://github.com/davidusb-geek/emhass/issues/826
**Audit source:** audits/2026-04-28-param-definitions.md
**Target repo:** `davidusb-geek/emhass` (via `OptimalNothing90/emhass` fork)
**Branch:** feat/ac-2a-unit-field
**Effort:** S
**Phase / Priority:** Phase 3 / P1
**Goal-fit:** LLM-ready

## 1. Problem

param_definitions.json describes EMHASS runtime parameters but units are currently embedded prose in description text. LLMs cannot reliably parse free-text units, blocking AGENTS-md-style automation. Issue #826 requests a structured `unit` field.

## 2. Goal

Adopt audit recommendation 1:1 — add a structured `unit` field to each parameter definition.

## 3. Decisions

| # | Decision | Source |
|---|----------|--------|
| 1 | Self-test artefact — content elided | _self-test_ |

## 4. Files touched

- src/emhass/static/data/param_definitions.json
- docs/ (update schema documentation if applicable)
- tests/test_command_line.py (smoke-check param_definitions parsing)

## 5. Concrete edits

| File | Edit type | Notes |
|------|-----------|-------|
| src/emhass/static/data/param_definitions.json | Add `unit` field to each entry | Self-test placeholder |
| docs/ schema docs | Update if schema documented | Self-test placeholder |

## 6. Test strategy

JSON schema validation in CI; existing `tests/test_command_line.py` smoke-checks param_definitions parsing.

## 7. Acceptance criteria

- Field present on every parameter
- JSON validates against existing schema
- Docs updated

## 8. Out of scope

Schema migration of older configs; UI changes

## 9. References

- Issue: https://github.com/davidusb-geek/emhass/issues/826
- Audit: audits/2026-04-28-param-definitions.md
- Memory: project_strategic_goals.md (LLM-ready stream)
