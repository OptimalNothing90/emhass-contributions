# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Editor
.vscode/
.idea/
*.swp
.DS_Store

# Repomix output (on-demand only, never committed)
repomix-output.*

# Build / Docker artefacts
*.tar
*.tar.gz
.docker/

# Local-only test data
data/
docker/dev-data/
contrib-flags.yaml         # only the example lives in repo; actual config lives in container volume
prototypes/flexd/flexd.yaml # only flexd.yaml.example lives in repo; e2e smoke copies it locally

# Claude Code per-user state (skills + shared settings ARE tracked)
.claude/settings.local.json
.claude/projects/
.tmp/
.worktrees/
